rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All reads and writes are performed by the trusted Express backend through
    // Firebase Admin SDK. Client-side access is intentionally closed.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

