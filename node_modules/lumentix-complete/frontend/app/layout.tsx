import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "LUMENTIX Solutions | AI-Driven Software Company",
  description: "AI-driven software products, automation systems, product strategy, and digital innovation from LUMENTIX Solutions."
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body>{children}</body>
    </html>
  );
}
