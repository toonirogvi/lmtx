import { VerificationClient } from "./verification-client";

export default async function VerifyPage({ params }: { params: Promise<{ reference_id: string }> }) {
  const { reference_id } = await params;
  return <VerificationClient referenceId={reference_id} />;
}
